<%- model_class = Ticket -%>

<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>

<div class="page-header">
  <h4><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %> (<span id="count_records"><%= @tickets.count %></span>)
	<div class="searchRight fr">
		<%= link_to t("general.search"), "javascript:void(0)", :class => 'btn  btn-small', 'id' => 'search_button' %>&nbsp;
	</div>
	</h4>
</div>

<div id="div_update_search">
  <% params[:model] = controller_name.classify %>
  <%= render "search" %>
</div>

<div id="div_update">
	<%= render "fetchAll" %>
</div>


